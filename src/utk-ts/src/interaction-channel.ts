export abstract class InteractionChannel {
    /**
     * Loads blablabla
     * @param {Function} myString blabla
     * @return { any } bla
     */

    static setInteraction(myString: Function): any {
        myString()

    }
}